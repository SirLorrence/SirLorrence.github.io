# Steps
How to get my development environment up and running. 

1. Ensure Hugo is installed.
2. Pull submodules: `git submodule update --init --recursive`
3. In project directory, run `hugo server -D`. This will start Hugo’s development server to view the site.
4. Reference the [Hugo documentation](https://gohugo.io/documentation/) for more information.
